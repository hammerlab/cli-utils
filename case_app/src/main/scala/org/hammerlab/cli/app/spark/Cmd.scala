package org.hammerlab.cli.app.spark

/*
import org.hammerlab.cli.app
import org.hammerlab.cli.app.Args
import org.hammerlab.cli.args.Parser

abstract class Cmd
  extends app.Cmd {
  val main = Main(makeApp)
  implicit def parser: Parser[Opts]
  def makeApp(args: Args[Opts]): app.App[Opts]
}
*/
